# Auto-generated by scripts/caddy-https.sh — do not edit; restart Caddy to refresh LAN IP.
# Flask: http://127.0.0.1:1337  →  Caddy: https://192.168.4.47:8443

{
	local_certs
	# Avoid binding :80 for HTTP→HTTPS redirects (not needed; phones use :8443).
	auto_https disable_redirects
	skip_install_trust
}

https://192.168.4.47:8443, https://localhost:8443, https://127.0.0.1:8443 {
	# Caddy defaults to IPv6-only on macOS; bind IPv4 so LAN/loopback HTTPS works.
	bind 0.0.0.0
	bind [::]
	reverse_proxy 127.0.0.1:1337
	tls internal
}
